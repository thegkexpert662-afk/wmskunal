import 'package:flutter/material.dart';

class StockMisScreen extends StatelessWidget {
  const StockMisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Stock / Excel MIS', style: TextStyle(fontSize: 28, fontWeight: FontWeight.w900, color: Color(0xFF10243E))));
  }
}
